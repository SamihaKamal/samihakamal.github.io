export function Projects(){
    return(
        // The idea is to have tiles for each project and clicking the tiles opens a modal with the project info
        <main>
            
        </main>
    )
    
}
